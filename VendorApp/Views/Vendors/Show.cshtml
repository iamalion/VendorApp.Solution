@{
    Layout = "_Layout";
}
    @using VendorApp.Models;

    <h3>Here are all the orders from this vendor:</h3>

    <ol>
    @foreach (Order order in (List<Order>)Model["order"])
    {
      <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Title</a></li>
    }
    </ol>

    <p><a href='/vendors/@Model["vendor"].Id/orders/new'>Add a new order</a></p>
    <p><a href='/categories'>Return to vendor list</a></p>
    <p><a href='/'>Return to Main Page</a></p>
